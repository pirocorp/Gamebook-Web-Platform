# Add Rule Type Prompt

Add a new game rule type.

Requirements:

1. Add strongly typed condition/effect model if needed.
2. Add implementation in `GameBook.Core` rules/effects engine.
3. Add tests.
4. Update `docs/game-engine/rules-engine.md`.
5. Do not implement authoritative rule logic in the frontend.
